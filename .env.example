# Example environment variables for Creative Hands
# Copy this file to `.env` and fill with your real secrets for local development.
# ⚠️ IMPORTANTE: El servidor NO iniciará si faltan las variables requeridas (fail-fast)

# ========================================
# VARIABLES REQUERIDAS (obligatorias)
# ========================================

# MongoDB connection (use MongoDB Atlas)
MONGODB_URI=mongodb+srv://usuario:password@cluster.xxxxx.mongodb.net/creative-hands?retryWrites=true&w=majority

# JWT settings
# ⚠️ Usar un secreto seguro en producción (mínimo 32 caracteres aleatorios)
JWT_SECRET=your_jwt_secret_here_minimum_32_chars
JWT_EXPIRE=7d

# ========================================
# VARIABLES OPCIONALES
# ========================================

# Server
PORT=5000
NODE_ENV=development

# Frontend URL (used by CORS)
CLIENT_URL=http://localhost:5173

# ========================================
# ADMIN POR DEFECTO (opcional - seguro)
# ========================================
# Para crear un administrador automáticamente al iniciar:
# 1. Establecer CREATE_DEFAULT_ADMIN=true
# 2. Configurar TODAS las variables ADMIN_* con valores seguros
# ⚠️ El servidor fallará si CREATE_DEFAULT_ADMIN=true pero faltan las credenciales
# ⚠️ NUNCA usar valores por defecto en producción

CREATE_DEFAULT_ADMIN=false
ADMIN_NAME=Administrador
ADMIN_EMAIL=admin@tudominio.com
ADMIN_PASSWORD=TuContraseñaSegura123!
